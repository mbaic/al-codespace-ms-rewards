# al-codespace-ms-rewards
A ready-to-fork Business Central AL extension (PTE) for testing in a BC Online sandbox via GitHub Codespaces — no local VS Code, no Docker, no Windows required. Built to reduce setup friction and share with others facing the same challenge.
